variables:
  ranks: [8] # , 16, 32]
  matrix: "./test_matrices/matrices_list.txt"
  args: "./args.txt"

command: "mpirun -np {ranks} --map-by :OVERSUBSCRIBE ./build/test_dmmio -f {matrix} {args}"

jobs:
  - config: "{ranks}_ranks"
    config_jobs:
      - tag: "test_dmmio"
